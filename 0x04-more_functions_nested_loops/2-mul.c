#include "holberton.h"

/*
 * mul - do the multiplication of two numbers
 * @a: first number
 * @b: second number
 * Return: returns the multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
